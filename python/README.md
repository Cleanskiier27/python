# Python Starter

This project uses a `src/` layout with a package named `python_starter`.

## Run

```bash
python -m python_starter.main
```

## Install

```bash
pip install -e .
```

Then run:

```bash
python -m python_starter.main
```
